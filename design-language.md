# 设计基因 · Design Language

> 这份文件回答"为什么这样设计"。规则可以照抄，但只有理解了基因，才能在规则没覆盖的新场景里做出不跑偏的判断。

## 核心命题：克制即高级

所谓"高级感"不来自更多装饰，而来自**更少的母题、更纯的色相、更稳的节奏**。整套系统建立在一个反直觉的取舍上：**能不加的就不加**。

三条不可动摇的信条：

### 1. 一套语言 × 多套主题

同一套组件结构，只换主题色，就能服务完全不同的公司与产品线。这意味着：
- 组件只吃一个 `theme` 参数，**结构零改动**；
- 任何"这家公司要换个样子"的诉求，答案都应该是**换主题令牌**，而不是重做版式；
- 这也是本系统作为"个人设计系统"的核心价值——你积累的是**语言**，不是某一家的稿子。

### 2. 证据优先：数据 > 形容词

B2B 产品页的读者是采购、研发、QA——他们不信形容词，只信可核查的事实。因此：
- **卖点用卡**（数字承诺）、**参数用表**（可逐行核对）、**证明用原始图**（仪器数据，不做美化）；
- 每个"快""高""稳定"都必须绑定一个数字（"快至 2 周""包封率 ≥90%"）；
- 结论横幅把数据翻译成一句话承诺，而不是喊口号。

### 3. 克制即高级：只有三个装饰母题

全稿**只允许**这三种几何装饰，再无其他：

| 母题 | 形态 | 出现位置 |
|---|---|---|
| 45° 胶囊棒 | 多档同色全圆角长条，斜穿出血、等距平行 | 仅封面 / 章节页 / 封底 |
| 右上 scoop 角 | 卡片容器右上角大圆角（其余角小圆角），方向全稿统一 | 卡片 / 流程容器 / 引文面板 |
| 全圆胶囊 | r = 高/2 的 stadium | 页题 / 结论横幅 / 时间轴色带 |

**除这三者之外的任何装饰，都是 AI slop 的入口。**

## 质感法则

高级感全部来自**同色相的明度阶梯**，而不是特效：

- **零阴影**：全稿不用投影。层次靠留白与色块深浅。
- **零渐变**：唯一例外是 tint 卡片内 15% 亮度以内的垂直微渐变（近乎不可察觉）。
- **零纹理、零半透明叠加、零发光边框**。
- 分割线只用 0.5pt 浅灰横线（表格行线），**区块之间靠 ≥ 40px 留白而非画线**。
- 圆角只有四档：胶囊（r=高/2）、scoop（右上 22~40px）、微圆（6~10px）、图片（0）。

## 视觉签名（一眼认出是"这套系统"的 5 个特征）

1. 结构页深底 + 45° 等距平行胶囊棒；
2. 内页白纸，主题色只以"实底胶囊 + 实底表头"两种形态出现；
3. 表格永远实底表头、无竖线、斑马纹极浅；
4. 卡片右上角那个大圆角（scoop）；
5. 页码是 `– 0X –` 短横线格式，奇偶左右交替。

## 与 "AI slop" 的对立

| AI slop 的做法 | 本系统的做法 |
|---|---|
| 蓝紫渐变 + 玻璃拟态 + 发光 | 纯色深底 + 明度阶梯 |
| emoji 当图标、图标风格混用 | 面性双色 SVG，一套字体感 |
| "居中大标题 + 灰副标题 + 双按钮" | 胶囊页题 + 卖点卡 + 参数表 |
| 每页都塞满、无处呼吸 | 章节闭环即留白，密度 3 页一循环 |
| 编造华丽形容词与假数据 | 只写可核查的数字，素材必须是真实的 |

详见 `references/anti-patterns.md`。
